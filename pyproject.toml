
[project]
name = "payroll_extensions"
version = "1.0.0"
description = "Auto-calculates late entries, early exits, and extended leaves"
authors = [{name = "Your Name", email = "your@email.com"}]
dependencies = ["frappe"]
